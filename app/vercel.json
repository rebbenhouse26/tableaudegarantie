{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "//": "Option : héberger UNIQUEMENT le frontend sur Vercel. Le backend + la base (données de santé) doivent rester sur un hébergeur HDS. Dans Vercel : Root Directory = app, définir VITE_API_URL = https://api.mon-domaine.fr/api.",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
